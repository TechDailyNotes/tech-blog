from enum import Enum


class Constant(Enum):
    ANNOTATIONS = "anno"
    DESIGN_PATTERNS = "pattern"
    EXCEPTION = "exception"
    GIT = "git"
    KEYWORDS = "key"
    REDIS = "redis"
    SOA = "soa"
    TESTING = "test"
