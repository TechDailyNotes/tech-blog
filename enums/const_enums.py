from enum import Enum


class Constant(Enum):
    ANNOTATIONS = "anno"
    DESIGN_PATTERNS = "pattern"
    EXCEPTION = "exception"
    GIT = "git"
    KEYWORDS = "key"
    QCONFIG = "qconfig"
    REDIS = "redis"
    SOA = "soa"
    TESTING = "test"
